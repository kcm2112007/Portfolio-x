android.useAndroidX=true
kotlin.code.style=official
# Add your real Supabase project values in a local.properties-based override,
# or pass as -PSUPABASE_URL=... -PSUPABASE_ANON_KEY=... — never commit real keys.
